# Log

Running record of setup and major activity.

## 2026-07-12
- Vault initialized at `C:\Users\Angela\Documents\Obsidian\Brain`.
- Phase 0 complete: git, node, npm, python, bun, ollama, Obsidian verified; nomic-embed-text pulled.
- Phase 1: five-pillar folder structure created.
- Phase 2: git + GitHub remote configured, Obsidian Git plugin set to auto commit-and-sync every 10 min, sync.ps1 scheduled every 20 min via Task Scheduler. Verified end-to-end.
- Phase 3: secrets.env created at C:\Users\Angela\.claude\secrets.env, permissions locked down, python-dotenv installed and verified.
- Phase 4: daily stack discovered (Gmail, Google Calendar, phone notes app, Canva, CapCut, Shopify, local file storage). Gmail/Calendar connectors registered but not yet surfacing as tools in this session (open item). No connectors/MCP found for Canva, CapCut, Shopify — logged as later builds in AI OS/Technology/Daily Stack.md.
- Phase 5: researched Metricool vs Buffer vs Later vs Planable, recommended Metricool free tier (best free analytics + TikTok/IG + calendar combo). Saved to AI OS/Technology/Social Media Tools.md. Weekly analytics review routine added. Metricool signup + TikTok/IG connection deferred to Open Items (Angela's choice).
- Phase 6: gbrain installed (bun install -g github:garrytan/gbrain), initialized with PGLite + local Ollama embeddings (nomic-embed-text, 768d), vault imported (9 pages), doctor health check passed (embeddings 100%, correct provider/dims). Registered as MCP server at user scope via standalone claude CLI (installed for this purpose). Search mode kept at conservative (no paid LLM expansion, $0 cost). gbrain guidance added to CLAUDE.md. Confirmed via `claude mcp list` that Gmail/Calendar connectors are genuinely healthy — the earlier "not appearing" issue is a session-scoping quirk, not a broken connection.
- Phase 7: full vault CLAUDE.md written (who Angela is, five pillars, wikilink conventions, filing rules, single-source-of-truth rule). Pointer CLAUDE.md added at C:\Users\Angela\CLAUDE.md.
- Phase 8: automode permissions written to C:\Users\Angela\.claude\settings.json (user scope) — standard ops (edits, builds, local git, gbrain MCP) proceed automatically; push, destructive resets, force branch delete, and file/folder deletion require confirmation; unlisted/future tools (e.g. social publishing) default to asking since defaultMode is "default".
- Phase 9: claude-mem@thedotmack plugin installed and enabled via standalone claude CLI (marketplace thedotmack/claude-mem added). Added an additional Stop hook (agent-type, in user settings.json) that writes a session summary into AI OS/Conversations/ each time a session ends, separate from claude-mem's own internal memory hooks. Verification deferred to next real session end (see Open Items).
- Phase 10: Telegram bot created via BotFather, token + chat ID saved to secrets.env. Bridge script (_sources/telegram_bridge.py) built with explicit UTF-8 handling, using `claude -p --resume` for session-persistent memory across messages. Verified end-to-end (test content idea correctly filed to Content/Ideas/). Windows Scheduled Task's -AtLogOn trigger is blocked in this environment, so used a Startup-folder shortcut + hidden VBScript watchdog instead (auto-restarts on crash). Fixed two bugs found during testing: subprocess FileNotFoundError launching claude.cmd on Windows (fixed via list2cmdline + shell=True), and a None sys.stdout crash when pythonw runs with no console (fixed with a None-guard). Wired a Telegram failure alert into sync.ps1 (also fixed a TLS 1.2 issue). Laptop-vs-VPS tradeoff documented in AI OS/Builds/.
- Phase 11: claude-mem + Marketing Skills enabled; ECC and C-Level Advisor installed but disabled (on-demand use); GStack cloned to C:\Users\Angela\tools\gstack, kept inactive until needed. Summary table saved to AI OS/Technology/Plugins and Skills.md.
- Phase 12: interviewed Angela on ventures (Astrology App — idea stage, freemium; Supplement Brand — idea stage, Shopify, early manufacturer conversations; Beauty Brand — sourcing samples, Shopify) and content (niches: lifestyle/travel/Chinese culture; posting a few times/week; handle ange.beee on both platforms; occasional brand deals; metrics: follower growth, views/engagement, saves/shares; workflow idea→plan/script→film→edit→post). Starter notes and folders created for all three ventures plus Content/index.md and Content/Platforms/Handles.md.
- FINAL: full checklist run — prereqs ✓, five pillars ✓, both auto-sync layers confirmed firing live during this session ✓, secrets.env gitignored + populated (Telegram token/chat ID) ✓, gbrain re-synced via MCP (19 pages, 100% embed coverage, 0 dead links) ✓, CLAUDE.md files ✓, automode permissions ✓, claude-mem + Stop hook confirmed working (multiple real summaries written) ✓, Telegram bridge confirmed auto-starting via Startup folder ✓, skills installed with correct enabled/disabled split ✓. Gmail/Calendar connectors healthy but not yet surfaced in this specific session (should resolve in a fresh session). Metricool signup deferred to Angela. Everything committed and pushed to GitHub.
- Post-setup: Angela signed up for Metricool and connected TikTok. Bug check found and fixed 3 issues: Telegram bridge had been killed during testing (restarted), duplicate bridge processes running simultaneously (deduped to one), secrets.env permissions had reset to Windows defaults (re-locked to Angela+SYSTEM only, documented as a recurring gotcha).
- New pillar added: **Investing/** — share investing/trading education across ASX, TWSE, HKEX, US. Built Education/Markets/Watchlist/Journal subfolders, free yfinance-based market_data.py and market_briefing.py tools, and a daily 8am Scheduled Task (AngeBrain-MarketBriefing) sending prices + news via Telegram — tested end-to-end including via the actual scheduled task (not just manual runs). Explicitly scoped as research/education only, no trade execution. CLAUDE.md updated.
